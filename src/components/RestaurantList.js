import React from 'react'
import {Text, StyleSheet, View} from 'react-native' 

const RestaurantList = () => {
    return<Text> Home Page Works !!</Text>
}

const styles = StyleSheet.create({
    container : {},
})

export default RestaurantList
